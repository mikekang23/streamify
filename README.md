A simple streaming app bare bones with authentication. One account can make multiple streaming channels. I used create-react-app to get started quickly.

To begin, after cloning, go to route directory:
### `yarn start`
